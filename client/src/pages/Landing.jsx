import HeroSection from "../components/landing/HeroSection";

const Landing = () => {
  return (
    <main className="relative min-h-screen w-full overflow-hidden bg-[#0B0C15] text-white">
      <HeroSection />
    </main>
  );
};

export default Landing;
